"use client";

// Onra Studio — Offerings settings landing route (admin chrome wraps it).
// UI-only studio-level on/off switches for the optional offering types
// (Memberships / Private sessions / Recovery). See OfferingsPage for details.
import OfferingsPage from "@/components/settings/OfferingsPage";

export default function AdminOfferingsRoute() {
    return <OfferingsPage />;
}
