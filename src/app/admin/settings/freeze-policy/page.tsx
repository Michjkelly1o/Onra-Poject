"use client";

// Onra Studio — Freeze Policy landing route (admin settings chrome wraps it via
// the settings layout's SettingsGroupHeader tab bar).
import FreezePolicyPage from "@/components/settings/FreezePolicyPage";

export default function AdminFreezePolicyRoute() {
    return <FreezePolicyPage />;
}
