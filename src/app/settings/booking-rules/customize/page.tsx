"use client";

// Onra Studio — Customize classes settings route (full-page modal, no admin chrome)
import { CustomizeClassesSettingsPage } from "@/components/settings/booking-rules/CustomizeClassesSettingsPage";

export default function CustomizeClassesSettingsRoute() {
    return <CustomizeClassesSettingsPage />;
}
