"use client";

// Onra Studio — Add new policy route (full-page modal)
import { PolicyFormPage } from "@/components/settings/booking-rules/PolicyFormPage";

export default function NewPolicyRoute() {
    return <PolicyFormPage />;
}
