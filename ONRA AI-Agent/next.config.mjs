/** @type {import('next').NextConfig} */
const nextConfig = {
  // mock-data/*.ts live outside /app and /src; nothing special needed —
  // they're imported via the "@/*" path alias.
};

export default nextConfig;
