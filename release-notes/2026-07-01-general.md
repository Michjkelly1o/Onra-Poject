Onra Prototype Update and Walkthrough
Overall modules done 14/14
This update covers these modules:
Tax
Referral
Branding
Instructor Profile

Note:
Today's feedback from the meeting/docs is not tackled yet and pushed to the Vercel version today, we only push what we have done this week and the previous week. In the prototype, maybe you will see some 404 pages; that means the page/module/feature is still being developed.
New side for admin: to navigate to admin please change the link to "https://onra-poject.vercel.app/admin/dashboard" and for the instructor side, please change it back to "https://onra-poject.vercel.app/instructor/dashboard"
New side for instructor: to navigate to instructor please change the link to "https://onra-poject.vercel.app/instructor/dashboard" and for the admin side, please change it back to "https://onra-poject.vercel.app/admin/dashboard"
Interactive experience — This is a fully interactive prototype. Feel free to click, create, edit, and delete items to test the workflows.
Live updates — The prototype uses realistic sample data. Any changes you make apply live as you navigate between different screens.
Session persistence — Any data you create, edit, or cancel survives a page refresh and tab close. Your demo session sticks until you explicitly reset.

1 Tax
The settings screen where the owner controls how tax is applied across the studio, split by VAT and Income so each surface only shows what it needs.
Capabilities:
VAT and Income tabs — One unified list for every tax rate, separated into a VAT view and an Income tax view so the owner sees only what's relevant.
Three rate types — Default, Zero-rated, and Exempt as selectable cards. The amount field only appears when it makes sense.
Per line item or per invoice — A single toggle decides whether tax is calculated on every line item or once on the invoice total.
Apply tax rates — A clear "Services" parent card groups Memberships, Credit packages, and Appointments, with a separate row for Gift cards and Pay rate. Each row can override the default.
Inheritance banner — A line at the top of Services reads the live VAT percentage so the owner sees at a glance what every category inherits.

2 Referral
The settings screen where the owner runs the rewards-based referral program end to end.
Capabilities:
Master switch — A single toggle turns the entire referral program on or off, with a confirmation before the flip.
Reward rules and limits — Side-panel editor that sets who the referrer earns, who the friend earns, the unlock trigger (sign up / first purchase / first class), the per-member referral cap, expiry days, and monthly program budget.
Eligibility and fraud controls — Side-panel editor with four guard rails: prevent self-referral, new customers only, require minimum first spend, and credits redeemable across all branches.
Customize referral information — Full-page editor with a title field, click-to-insert variables ({{referrer}} / {{friend}} / {{trigger}} / {{cap}}), a rich text editor, and a live preview of exactly what the customer will see.
Customer detail Referrals tab — Three summary cards (referral code, total referrals out of the cap, total bonus credits earned), plus a sortable table with an Expiry date column so the owner can see when each referred reward runs out.
Branch-locked credits — When "Credits redeemable across all branches" is off, the referred customer's credits are locked to the branch of the referrer. The customer detail Referrals tab surfaces a small amber line under each row so the owner sees the lock at a glance.

3 Branding
The settings screen where the owner customizes the studio's customer-facing look and feel.
Capabilities:
Three-step Customize design — Step through brand colors, typeface, and app icons. Each step shows a fitted phone preview that updates as the owner edits.
Six fonts to choose from — Pick the typeface that fits the brand; the preview switches font live.
Login, Home, and Class previews — Polished previews so the owner can confirm the brand reads correctly on the screens customers see first.
Portal preferences sub-page — Toggles for the customer experience (light / dark default, social handles, support email).

4 Instructor Profile
The instructor's own account settings screen. Same visual language as the admin Account settings.
Capabilities:
Personal information, Integrations, Notification settings tabs — Everything the instructor needs to configure lives under one screen.
Password section — Reveals the current password with an eye toggle. A subtle info line underneath shows the last change date and how long ago it was ("Last changed Mar 14, 2026 · 109 days ago") so the instructor knows when it's time to rotate.
Active sessions — Lists every device signed in, with the current one tagged with a green "Current" pill and any other device given a red "Sign out" action that removes it from the list with a confirmation toast.
Working days — Weekday letters (M–T–W–T–F–S–S) highlight the days the instructor is available; the off days read in red so scheduling conflicts are obvious.

General updates
Settings gear icon — The Settings link in the sidebar now uses the classic gear icon, making it easier to spot at a glance.
Sidebar divider — The section break between the top of the sidebar and the studio-scoped modules is now a clean horizontal line instead of a "STUDIO" text label. Reads quieter.
Roles & Permissions cleanup — The Export button was removed from the Roles page since role definitions aren't the kind of data owners typically export.
Password history on admin Account settings — Same "Last changed" info line under the password value plus the Active sessions list, matching the new instructor Profile so both personas share one pattern.
