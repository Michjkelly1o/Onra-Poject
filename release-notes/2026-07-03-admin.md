Onra Admin Prototype Update and Walkthrough
Overall modules done 14/14
This update covers these modules:
Customer Management
Customer Notifications
General Settings

Note:
Today's feedback from the meeting/docs is not tackled yet and pushed to the Vercel version today, we only push what we have done this week and the previous week. In the prototype, maybe you will see some 404 pages; that means the page/module/feature is still being developed.
New side for admin: to navigate to admin please change the link to "https://onra-poject.vercel.app/admin/dashboard" and for the instructor side, please change it back to "https://onra-poject.vercel.app/instructor/dashboard"
Interactive experience — This is a fully interactive prototype. Feel free to click, create, edit, and delete items to test the workflows.
Live updates — The prototype uses realistic sample data. Any changes you make apply live as you navigate between different screens.
Session persistence — Any data you create, edit, or cancel survives a page refresh and tab close. Your demo session sticks until you explicitly reset.

1 Customer Management
The customer list + detail screen where the owner manages every customer and how they hear from the studio.
Capabilities:
Marketing preferences — Expanded from 3 opt-ins to 8 on the customer Details tab. Four delivery channels (Marketing emails, Marketing WhatsApp, Marketing SMS, Push notifications) and four content topics (Studio announcements, New class launch, Special offers, Promo code offers). Each row shows Subscribed with a green check or Unsubscribed with a red cross so the owner sees at a glance what a customer wants to receive.
Realistic customer counts — The demo now runs on around 1,500 customers instead of a small handful, so opt-in counts, dashboard tiles, and every "how many customers" number in the app reads like a real studio.
All locations default — The branch filter at the top of the customer list opens on "All locations" so the module lands on the aggregate view. Drill into a specific branch when needed.

2 Customer Notifications
The settings screen where the owner controls every message the customer receives — which channels each event goes out on, what the message says, and when it's sent.
Capabilities:
Redesigned list — One clean table with a row per event and columns for Email, WhatsApp, Approval status, SMS, Send time, and a kebab menu. Five collapsible groups on the page: Booking, Payment, Package & Membership, Marketing & Promotions, and Referral. Each row shows a Critical pill on payment events + a "Sent during campaigns" pill on marketing rows.
Payment protection — The owner can turn any single channel off, but the last remaining channel on a payment row is locked on. If the owner tries to switch it off anyway, an amber toast pops up ("[Event name] is critical") explaining that at least one channel must stay live so paying customers always get told.
WhatsApp connection gate — The whole WhatsApp column, Approval status column, and message editor tab react live to whether the WhatsApp Business integration is connected. When it's disconnected, the column grays out, approval pills hide, and a tooltip on the column header explains how to reconnect. Re-connect from Settings > Integrations and everything comes back.
Approval status — Every WhatsApp template shows an Approved / Pending / Rejected pill that mirrors Meta's real approval workflow. Editing a WhatsApp template body automatically flips its status back to Pending (matches Meta's rule that any content change triggers re-review) and fires a "resubmitted to Meta" toast.
Message editor — Click Edit template on any row to open a four-tab modal: Email, WhatsApp, SMS, and Manage timing. Each channel tab carries a variable rail (click to insert), a live preview with sample data, and a "Send a test to yourself" row. The modal stays a constant size when switching tabs — no more jumping when a tab has less content.
Send timing — On the Manage timing tab the owner picks Immediately or Scheduled. Scheduled sends can carry multiple offsets ("24 hours before", "2 hours before") stacked as a list. Save is disabled until at least one valid offset is entered, so the owner can't accidentally save a scheduled send with no time set.
Delivery hours — A "Delivery hours" button at the top of the page opens a side panel where the owner sets a quiet window (default 21:00–07:00) and chooses whether critical payment messages bypass it. The panel explains that if the end time is earlier than the start, the window wraps overnight.
Marketing opt-in banner — The Marketing & Promotions group ends with a green banner reading "Only X of Y customers have opted in to receive marketing messages." The numbers are live counts of the customer preferences from the Customers module, so it reads like real studio data (currently 1,073 of 1,377).

3 General Settings
The Settings hub reorganised into three groups so the owner spends less time hunting for a screen.
Capabilities:
Three-group organisation — Every settings screen now sits under one of three tabs: Business (Business & locations, Branding), Operations (Booking rules, Tax, Integrations), or Customer (Customer notifications, Agreements). The tab bar sits at the top of each screen so the owner always sees where they are within the group.
Sidebar dropdown — Clicking the Settings gear at the bottom of the sidebar opens a small dropdown showing Business, Operations, and Customer with their own icons. Same pattern as the profile chip.
Tax module — Added a Tax Registration Number (TRN) card at the top of the VAT tab with the studio's TRN number, issuing country, and a toggle to show it on customer invoices. Every tax rate now carries an effective window (Valid from and Valid until) shown as a new column on the list ("DD/MM/YYYY - DD/MM/YYYY" or "Ongoing" when open-ended). The Add / Edit tax rate screen picked up matching date pickers at the bottom.
Agreements module — New Last signed column on the agreements list showing when each agreement was most recently accepted by anyone. Opening an agreement now lands on the Acceptance status view — the three big numbers (Signed on current version / On older version / Pending or never signed) plus the customer table are the first thing the owner sees.
Payroll — Instructor detail page renamed to "Payroll Details" so the title matches what the screen actually shows. Tax withholding on payroll is now country-aware: hidden for UAE and other GCC studios, reappears automatically if the studio switches to a country that does apply income tax. The payroll CSV export now breaks each instructor down into typed rows (Pay model, Component, Basis, Rate, Amount) with an explicit Subtotal for multi-component pay models, replacing the free-text explanation column that used to be hard to read in Excel.

General updates
Sidebar accordion — Clicking a menu that has a submenu now automatically collapses every other expandable group. Only one submenu stays open at a time so the sidebar reads clean regardless of where the owner is.
Sub-menu hierarchy — Sub-menu rows sit clearly indented under their parent group with a consistent visual step, so which module a screen belongs to reads at a glance.
Branch filter defaults — Every list module now defaults its branch filter to "All locations" — Customers, Promotions, Marketing, Schedule, POS, Dashboard, Pay rates, and In-app notifications all open on the aggregate view. Pick a specific branch when you need to drill in.
Module renames — "Promo codes" is now "Promotions" everywhere the module surfaces (sidebar, page title, toolbar, toasts). The role label on the account/profile screen reads "Owner" instead of "Super admin" so the persona matches what the demo represents.
